% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getmethods.R
\name{get_model.spar.cv}
\alias{get_model.spar.cv}
\title{Get Best Model for \code{spar.cv} Objects}
\usage{
\method{get_model}{spar.cv}(
  object,
  opt_par = c("best", "1se"),
  x = NULL,
  y = NULL,
  xval = NULL,
  yval = NULL,
  ...
)
}
\arguments{
\item{object}{An object of class \code{spar.cv}, typically the result of \code{spar.cv()}.}

\item{opt_par}{A character string specifying the selection criterion:
\itemize{
\item \code{"best"}: Selects the model with the minimum validation measure.
\item \code{"1se"}: Selects the simplest model within 1 standard error of the best model.
}}

\item{x}{A matrix or data frame of predictors. Required if \code{fast_fit != "fix_rpm_and_inds"}.}

\item{y}{A response vector. Required if \code{fast_fit != "fix_rpm_and_inds"}.}

\item{xval}{A matrix or data frame of validation predictors. If \code{NULL}, \code{x} is used.}

\item{yval}{A validation response vector. If \code{NULL}, \code{y} is used.}

\item{...}{Additional arguments passed to \code{spar()} for re-estimation.}
}
\value{
An updated \code{spar} or \code{spar.cv} object containing:
\itemize{
\item \code{betas}: Coefficients of the selected model.
\item \code{intercepts}: Intercepts for the selected model.
\item \code{val_res}: Validation results filtered for the selected \code{nummod} and \code{nu}.
\item Additional fields like \code{xscale}, \code{yscale}, \code{xcenter}, and \code{ycenter} if \code{fast_fit == "fix_rpm_and_inds"}.
}
}
\description{
Extracts the best or 1SE model from a fitted \code{spar.cv} object.
The selection is based on cross-validation results, and the model can be re-estimated if required.
}
\examples{
fitted_spar_cv <- spar.cv(x, y, family = gaussian(), nfolds = 3L)
best_spar_cv <- get_model(fitted_spar_cv, opt_par = "best")
}
