% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparmodel_classes.R
\name{sparmodel-class}
\alias{sparmodel-class}
\title{SPAR Model Object Class}
\description{
The \code{sparmodel} class represents a configuration for \strong{fitting marginal models} in the
\strong{SPAR (Sparse Projected Averaged Regression)} framework. Objects of this class encapsulate:
\itemize{
\item Functions for estimating marginal models (e.g., GLM, GLMNET, or custom models).
\item Control parameters for the model fitting process.
\item Metadata (e.g., name, attributes) for customization.
}

Marginal models are fitted to \strong{projected predictors} (after screening and random projection) to compute
coefficients and intercepts for each model in the ensemble.
}
\details{
Objects of class \code{sparmodel} are created using the \code{constructor_sparmodel} function or predefined constructors
like \code{spar_glm} and \code{spar_glmnet}. They are used in the \code{spar} and \code{spar.cv} functions to define how marginal models
are fitted to the projected data.

The class includes the following components:
\itemize{
\item \strong{\code{name}}: A character string describing the model type (e.g., \code{"glm"}, \code{"glmnet"}, \code{"glmrob"}).
\item \strong{\code{generate_fun}}: A function to estimate the marginal model coefficients and intercept. This function must accept:
\itemize{
\item \code{object}: An object of class \code{sparmodel}.
\item \code{x}: A matrix of standardized predictors (unused in most cases, as models are fitted on projected data).
\item \code{y}: A vector of standardized responses.
\item \code{z}: A matrix of projected predictors (the design matrix for the marginal model).
\item \code{...}: Additional arguments passed from \code{spar()}.
The function must return a list with two elements:
\itemize{
\item \code{gammas}: A vector of regression coefficients for the projected predictors.
\item \code{intercept}: The intercept of the model.
}
}
\item \strong{\code{update_fun}}: A function to update the \code{sparmodel} object with data-specific information before fitting.
This function must accept:
\itemize{
\item \code{object}: An object of class \code{sparmodel}.
\item \code{x}: A matrix of standardized predictors.
\item \code{y}: A vector of standardized responses.
\item \code{family}: A \code{\link[stats:family]{stats::family}} object.
\item \code{...}: Additional arguments passed from \code{spar()}.
If not provided, the default \code{update_sparmodel_default} is used, which only updates the \code{family} if missing.
}
\item \strong{\code{control}}: A list of control parameters for the model fitting process (e.g., \code{family}, \code{alpha} for \code{glmnet}).
}
}
\section{Model Types}{

The following predefined model types are available:
\itemize{
\item \strong{\code{spar_glm}}: Fits marginal models using \code{\link[stats:glm]{stats::glm}}. Supports all standard GLM families.
For Gaussian models with identity link, it uses a fast OLS solver.
\item \strong{\code{spar_glmnet}}: Fits marginal models using \code{\link[glmnet:glmnet]{glmnet::glmnet}}. Supports penalized regression (Ridge, Lasso, or Elastic Net).
Automatically converts \code{family} objects to strings for compatibility with \code{glmnet}.
\item \strong{Custom Models}: Users can define their own model types by providing custom \code{generate_fun} and \code{update_fun}
functions to \code{constructor_sparmodel}. For example, \code{spar_glmrob} (not exported by default) uses
\code{\link[robustbase:glmrob]{robustbase::glmrob}} for robust regression.
}
}

\examples{
model_glmnet <- spar_glmnet(alpha = 0.5)

example_data <- simulate_spareg_data(n = 200, p = 2000, ntest = 100)
spar_res <- spar(
  example_data$x,
  example_data$y,
  xval = example_data$xtest,
  yval = example_data$ytest,
  model = model_glmnet
)

}
\seealso{
\code{\link{constructor_sparmodel}}, \code{\link{spar_glm}}, \code{\link{spar_glmnet}}, \code{\link{spar}}
}
