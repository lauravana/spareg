% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rp_classes.R
\name{randomprojection-class}
\alias{randomprojection-class}
\title{Random Projection Object Class}
\description{
The \code{'randomprojection'} class' represents a configuration for generating and managing random projection matrices in the \strong{SPAR (Sparse Projected Averaged Regression)} framework. Objects of this class encapsulate:
\itemize{
\item Functions for generating, updating, and modifying random projection matrices.
\item Control parameters for the random projection process.
\item Metadata (e.g., name, attributes) for customization.
}
}
\details{
Objects of class \code{'randomprojection'} are created using the \code{constructor_randomprojection} function. They are used in the \code{spar} and \code{spar.cv} functions to define how predictors are projected into a lower-dimensional space.

The class includes the following components:
\itemize{
\item \strong{\code{name}}: A character string describing the random projection method (e.g., \code{"rp_gaussian"}, \code{"rp_sparse"}).
\item \strong{\code{generate_fun}}: A function to generate the random projection matrix. This function must accept arguments like \code{object}, \code{x}, \code{y}, \code{m}, \code{included_vector}, and \code{...}.
\item \strong{\code{update_fun}}: A function to update the \code{randomprojection} object with data-specific information. This function must accept arguments like \code{object}, \code{x}, \code{y}, \code{family}, and \code{...}.
\item \strong{\code{update_rpm_w_data}}: A function to update an already-generated random projection matrix with data-dependent information. This function must accept arguments like \code{rpm}, \code{object}, \code{included_vector}, \code{x}, \code{y}, \code{family}, and \code{...}.
\item \strong{\code{control}}: A list of control parameters for the random projection process (e.g., \code{mslow}, \code{msup}, \code{psi}).
}
}
\section{Attributes}{

The following attributes are commonly used in \code{randomprojection} objects:
\itemize{
\item \strong{\code{mslow}}: Integer. The minimum dimension for projection. Default: \verb{\eqn{\log(p)}}.
\item \strong{\code{msup}}: Integer. The maximum dimension for projection. Default: \verb{\eqn{n/2}}.
\item \strong{\code{data}}: Logical. If \code{TRUE}, the projection matrix is updated with data-dependent coefficients (e.g., for \code{rp_cw}).
}
}

\section{Usage}{

\code{'randomprojection'} objects are typically created using predefined constructors like:
\itemize{
\item \code{rp_gaussian()}: Gaussian random projection.
\item \code{rp_sparse()}: Sparse random projection (Achlioptas, 2003).
\item \code{rp_cw()}: Sparse Embedding (Clarkson-Woodruff) random projection.
}

Users can also define custom random projection methods by providing their own \code{generate_fun}, \code{update_fun}, and \code{update_rpm_w_data} functions to \code{constructor_randomprojection}.
}

\examples{
rp_gauss <- rp_gaussian(mslow = 5, msup = 10)
example_data <- simulate_spareg_data(n = 200, p = 2000, ntest = 100)
spar_res <- spar(
  example_data$x,
  example_data$y,
  xval = example_data$xtest,
  yval = example_data$ytest,
  rp = rp_gauss
)

}
\references{
{
\insertRef{ACHLIOPTAS2003JL}{spareg}

\insertRef{Clarkson2013LowRankApprox}{spareg}

\insertRef{parzer2024glms}{spareg}.
}
}
\seealso{
\code{\link{constructor_randomprojection}}, \code{\link{rp_gaussian}}, \code{\link{rp_sparse}}, \code{\link{rp_cw}}, \code{\link{spar}}
}
