% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getmethods.R
\name{get_model.spar}
\alias{get_model.spar}
\title{Get Best Model for \code{spar} Objects}
\usage{
\method{get_model}{spar}(object, ...)
}
\arguments{
\item{object}{An object of class \code{spar}, typically the result of \code{spar()}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
An updated \code{spar} object containing:
\itemize{
\item \code{betas}: Coefficients of the best model, with values below \code{nu} set to 0.
\item \code{intercepts}: Intercepts for the selected number of models (\code{nummod}).
\item \code{val_res}: Validation results filtered for the best \code{nummod} and \code{nu}.
}
}
\description{
Extracts the best model from a fitted \code{spar} object based on validation results.
The best model is determined by the minimum validation measure.
}
\examples{
example_data <- simulate_spareg_data(n = 200, p = 400, ntest = 100)
fitted_spar <- spar(example_data$x, example_data$y, family = gaussian())
best_spar <- get_model(fitted_spar)
}
